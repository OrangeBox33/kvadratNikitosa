# TODO — включить OTA (httpUpdate)

Код уже написан (клиент, сервер, скетч). Осталось сделать руками:

## Инфраструктура
- [ ] Настроить в nginx раздачу `.bin` по **плоскому http** (без редиректа на https):
      ```nginx
      location /fw/ { root /var/www; }   # -> /var/www/fw/kvadrat.bin
      ```
      Проверить, что `http://93.183.69.206/fw/kvadrat.bin` отдаётся напрямую, а не через `301 -> https`.
- [ ] Убедиться, что `firmwareUrl` в скетче совпадает с этим URL.

## Прошивка (Arduino IDE)
- [ ] Tools -> Partition Scheme -> **Minimal SPIFFS (1.9MB APP with OTA / 190KB SPIFFS)**.
- [ ] Собрать скетч, проверить что бинарь влезает в слот.
- [ ] **Первый раз прошить панель по кабелю** (чтобы на ней уже был OTA-приёмник).
- [ ] Положить свежий `kvadrat.bin` в `/var/www/fw/`.

## Проверка OTA по воздуху
- [ ] Зайти на `/update/<панель>` (напр. `/update/sega`), нажать «Обновить».
- [ ] По Serial проверить: `OTA: старт обновления прошивки` -> `OTA OK` -> ребут.
- [ ] Если `OTA failed` — смотреть код ошибки в Serial (частая причина: редирект на https или файл не найден).

## Деплой кода (клиент + сервер)
- [ ] Пересобрать клиент (`npm run build`) и выложить.
- [ ] Пересобрать сервер (`cd server && npm run build`), запускать `dist/server/server.js`.
      Перед сборкой удалить старый `server/dist`.

## Опционально (на потом)
- [ ] Версионирование: эндпоинт `/fw/version` + сравнение в скетче, чтобы не шиться вхолостую.
